# Reviewer service

Run locally with `uv run uvicorn reviewer.main:app --reload`. The `docling`
dependency group is reserved for the high-fidelity parser evaluation described
in the roadmap; the working default stays lightweight enough for laptops and CI.
