{
  "name": "reviewer-agent",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "relay": "npm --prefix frontend run relay",
    "test:web": "npm --prefix frontend run typecheck"
  },
  "engines": {
    "node": ">=22"
  }
}
